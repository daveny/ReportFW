{{
  query="
SELECT 2023 AS OrderYear
CROSS JOIN (SELECT 'FR' AS CountryRegionCode) c
CROSS JOIN (SELECT 120000 AS TotalSales) t
";
  representation="barchart";
  series="TotalSales";
  legends="OrderYear";
  groupby="CountryRegionCode";
  formatting={ title: "Smoke test – static" };
}}